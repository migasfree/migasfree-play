export function getCategories(state) {
  return state.categories
}

export function selectedCategory(state) {
  return state.selectedCategory
}

export function searchApp(state) {
  return state.searchApp
}

export function onlyInstalledApps(state) {
  return state.onlyInstalledApps
}

export function searchDevice(state) {
  return state.searchDevice
}

export function onlyAssignedDevices(state) {
  return state.onlyAssignedDevices
}
